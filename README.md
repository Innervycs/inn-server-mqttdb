# IoT Stack on Ubuntu 24.04

```bash
# 1) Clone & configure
$ git clone https://github.com/<your-user>/iot-stack.git
$ cd iot-stack && cp .env.sample .env   # edit secrets

# 2) Install (needs root)
$ sudo bash install.sh